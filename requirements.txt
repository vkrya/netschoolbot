# Telegram-бот
aiogram>=3.4.0

# Веб и мини-приложение (один event loop с ботом, без Flask и eventlet)
aiohttp>=3.9.0
aiohttp-jinja2>=1.6
jinja2>=3.1.0

# Хранилище
aiosqlite>=0.20.0

# «Сетевой город»
netschoolpy==3.3.3
httpx[socks]
aiohttp-socks>=0.8.0
marshmallow
marshmallow_dataclass
typing-inspect

# QR-код для входа через Госуслуги
qrcode[pil]>=7.0
Pillow

# Web push
pywebpush>=2.0.3

# Конфигурация
python-dotenv>=1.0.0
